# 视觉规范

FreshLi4 Project Graph 是 Godot 编辑器的一部分，优先继承当前 Editor Theme。

## 视觉主题

- 关键词：工程化、克制、可扫描、类型明确。
- 整体气质：接近 Godot 原生调试工具和节点编辑器。
- 应避免：高饱和霓虹、大面积渐变、装饰性阴影。

## 色彩

- 背景、表面、正文、边框：继承 Godot Editor Theme。
- Scene：`#5DA9E9`
- Script：`#F2C94C`
- Resource：`#BB86FC`
- Mesh：`#56CC9D`
- Texture：`#F2994A`
- Audio：`#EB6F92`
- Shader：`#6FCFDE`
- Data：`#A0AEC0`
- Missing / error：编辑器错误色，回退为 `#EB5757`

颜色只用于辅助区分，节点必须同时显示文字类型。

### 关系线

- 静态引用：`#B8C0CC` 灰色实线箭头，source 指向 target。
- GDScript 继承：`#6FCFDE` 青色实线箭头，子类指向父类。
- 推断 / 动态：`#F2B84B` 琥珀色虚线箭头，表示不是精确静态依赖。
- 蓝色 `#5DA9E9` 只表示 Scene 节点，不表示方向、选中或引用类型。
- 颜色、箭头和线型必须同时在 **Legend…** 中用文字解释，不能要求用户猜测。

## 字体

- 字体：继承 Godot Editor Theme。
- 文件名：使用默认字体并允许按任意字符换行，以兼容没有空格的长文件名。

## 间距与布局

- 基础间距单位：`4 px`
- 工具栏控件间距：`8 px`
- 图节点固定尺寸：`320 × 320 px`，保持正方形比例。
- 节点间距：根据节点卡片尺寸和碰撞留白计算，不使用固定列间距。

## 组件风格

- 工具栏：单行排列 Scan、Organic Layout、Ignore、Export、Search 与统计信息。
- 图节点：大面积标题区显示完整文件名，底部短条只显示类型和缺失状态，不显示 `res://` 路径。
- 文件名使用普通 Label，不放入滚动容器；在固定宽度内按任意字符自动换行，并保留至少三行文字高度。
- 资产类型短条高度为 `42 px`，标题区高度为 `248 px`；节点内容控件不截获鼠标事件。
- 资源路径只用于搜索、打开资产和 FileSystem Dock 定位，不作为卡片正文。
- 搜索框：即时过滤节点；空字符串恢复全部节点。
- 空状态：提示用户点击 Scan。
- Missing 节点：保留路径并使用错误色。
- Ignore 对话框：明确区分不可移除的默认排除项与项目级自定义模式。

## 布局

- 默认使用确定性的离线有机力导向布局，不依赖持续动画或随机初始值。
- 连通节点按连接度从高到低以黄金角螺旋初始化，使高连接度节点先进入圆心附近，并在初始状态就占用圆盘内部。
- 固定步数的连线弹簧、节点排斥、中心重力和阻尼让结构向圆形团簇收敛；根据节点卡片总面积推导软圆盘边界，把过长分支弯回圆盘内部；大图使用 Barnes–Hut 四叉树近似排斥。
- 直接连线的目标长度以卡片包围尺寸加净空为基准；两个节点共享邻居时增加较弱的二阶吸引，使结构相近的资产形成局部密度，而不是被全局排斥均匀打散。
- 继承层级优先于普通连接度：继承链中越靠上的父类中心权重越高；其余节点按连接度获得中心吸引。
- 碰撞约束按节点卡片矩形和留白做最终松弛，不允许卡片相交。
- 卡片之间保留至少 `64 px` 可见净空，使箭头和关系线不会被相邻卡片完全遮住。
- 力模拟收敛后按半径带执行确定性的角向候选交换；只接受交叉数降低且平均边长受控，或交叉数不变但总边长缩短的交换。交换复用已有位置，因此保持卡片净空、圆盘形状和中心层级。
- 语义边以中心直线为首选；当直线穿过第三方卡片时，使用带 `14 px` 安全边距的矩形可见性图寻找最短折线路径。候选路径的每一段都必须通过全部可见卡片的障碍检测，拖动节点或筛选后重新计算。
- 零连接节点不参与连通图松弛，稳定分布在连通团簇包围半径之外；只有独立节点时保留空心中心。
- 不保证任意图平面化；非平面关系仍然必然交叉，但必须统计并减少当前几何布局中可避免的交叉。边可以交叉，不能被无关节点卡片遮挡。
- 扫描完成和手动恢复布局后自动缩放、居中；用户仍可继续拖动、缩放和平移。

## 交互

- 单击：选择节点并显示 Godot 原生选中样式。
- 双击：在 Godot 中打开场景、脚本或资源。
- 选中节点后按住鼠标左键拖动：临时调整节点位置。
- 在画布空白处按住鼠标左键拖动：平移视图。
- 节点右键：显示 **Show in FileSystem**，在 Godot 原生 FileSystem Dock 中定位文件。
- 缩放与 minimap：沿用 `GraphEdit` 原生交互。

## 可访问性

- 类型必须有文字标签。
- 控件保留键盘焦点状态。
- 状态消息不只依赖颜色。
- 不使用持续动画。
