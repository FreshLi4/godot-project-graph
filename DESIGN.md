# 视觉规范

FreshLi4 Project Graph 是 Godot 编辑器的一部分，应优先继承当前 Editor Theme，而不是建立一套与编辑器冲突的独立皮肤。

## 视觉主题

- 关键词：工程化、克制、可扫描、类型明确。
- 整体气质：接近 Godot 原生调试工具和节点编辑器。
- 应避免：高饱和霓虹、大面积渐变、装饰性阴影和仿网页营销组件。

## 色彩

- 背景、表面、正文、边框：继承 Godot Editor Theme。
- Scene：`#5DA9E9`。
- Script：`#F2C94C`。
- Resource：`#BB86FC`。
- Mesh：`#56CC9D`。
- Texture：`#F2994A`。
- Audio：`#EB6F92`。
- Shader：`#6FCFDE`。
- Data：`#A0AEC0`。
- Missing / error：使用编辑器错误色，回退为 `#EB5757`。

颜色只用于辅助区分，节点必须同时显示文字类型，不能只依赖颜色传达语义。

## 字体与间距

- 字体：继承 Godot Editor Theme。
- 路径：使用编辑器等宽字体能力可用时的等宽样式；否则使用默认字体。
- 基础间距单位：`4 px`。
- 工具栏控件间距：`8 px`。
- 图节点最小宽度：`260 px`。
- 节点间距必须根据节点卡片尺寸和碰撞留白计算，不使用固定列间距。

## 组件

- 工具栏：单行排列 Scan、Organic Layout、Ignore、Export、Search 与统计信息。
- 图节点：标题显示文件名，正文显示类型和完整 `res://` 路径。
- 搜索框：即时过滤节点；空字符串恢复全部节点。
- 空状态：明确提示用户点击 Scan，不自动扫描大型项目。
- Missing 节点：保留路径并使用错误色。
- Ignore 对话框：明确区分不可移除的默认排除项与项目级自定义模式。

## 布局

- 默认使用确定性的离线有机力导向布局，不依赖持续动画或随机初始值。
- 连通节点按连接度从高到低以黄金角螺旋初始化，使高连接度节点先进入圆心附近，并在初始状态就占用圆盘内部。
- 固定步数的连线弹簧、节点排斥、中心重力和阻尼让结构向圆形团簇收敛；根据节点卡片总面积推导软圆盘边界，把过长分支弯回圆盘内部；大图使用 Barnes–Hut 四叉树近似排斥。
- 连接度最高的节点固定在原点；其他高连接度节点承受更强的中心吸引，逐步向外过渡到叶节点。
- 碰撞约束按节点卡片矩形和留白做最终松弛，不允许卡片相交。
- 零连接节点不参与连通图松弛，稳定分布在连通团簇包围半径之外；只有独立节点时保留空心中心。
- 不保证边平面化；交叉引用仍可能产生交叉线，但节点应尽可能填满近圆形范围，而不是排成圆环。
- 扫描完成和手动恢复布局后自动缩放、居中；用户仍可继续拖动、缩放和平移。

## 交互

- 单击：选择节点。
- 双击：在 Godot 中打开场景、脚本或资源。
- 拖动：允许用户临时调整布局。
- 平移、缩放与 minimap：沿用 `GraphEdit` 原生交互。

## 可访问性

- 类型必须有文字标签。
- 控件保留键盘焦点状态。
- 状态消息不只依赖颜色。
- 不使用持续动画；扫描完成只更新文字和图内容。
