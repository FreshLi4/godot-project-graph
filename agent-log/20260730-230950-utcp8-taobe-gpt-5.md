# 画布拖拽、文件定位与节点避障连线

## 运行信息

- 用户原始 prompt：`移动做成按住拖拽；不需要显示文件的位置，显示完整的文件名称，宽度不变，超长换行；邮件可以打开文件所在位置（原生编辑器就有的那个 show in filesystem）；避让做的还是一般，交叉还是有点多，我觉得你至少要保证线不会被 Block 遮挡？`
- 模型：GPT-5。当前运行环境只暴露到该模型标识，无法取得更细的公开版本号。
- 启动分支与版本：`main@8e355441e9e9`，与 `origin/main` 同步。
- 任务开始时间：2026-07-30 23:00:02 +0800。
- 任务结束时间：2026-07-30 23:09:50 +0800。
- 是否提交：是；按用户既有要求直接提交并推送 main。

## 已阅读上下文

- `AGENTS.md`、`README.md`、`REQUIREMENTS.md`、`DESIGN.md`。
- `docs/ARCHITECTURE.md` 与上一轮关系聚类/交叉优化日志。
- Godot 4.4 官方 `EditorInterface.select_file` 与稳定版 `GraphEdit` 交互文档。

## 对话与行动记录

- 确认现有语义层只裁剪起终点，直线中段会穿过并被第三方 GraphNode 遮挡。
- 将“边与边交叉”和“边穿过节点”拆成软优化与硬约束；本轮以节点避障为硬验收。
- 画布空白处增加左键按住拖拽平移；节点继续使用 GraphNode 原生拖拽。
- 用户原文中的“邮件”结合 “Show in FileSystem” 语义按“右键”实现：节点右键菜单通过插件信号调用 `EditorInterface.select_file()`。
- 移除卡片中的 `res://` 路径、标题省略和横向跑马灯，改为固定宽度内按任意字符换行；使用固定高度滚动容器防止极长文件名撑大卡片。
- 语义边路由先尝试直线；被第三方卡片阻挡时，以放大 `14 px` 的矩形角点建立可见性图，并增量发现次级障碍。最短路径的每一段都要再次通过全部可见卡片检测。

## 完成工作

- 新增画布左键拖拽平移与缩放归一化偏移计算。
- 节点卡片只显示完整文件名、资产类型和缺失状态；保留路径作为搜索与导航元数据。
- 节点右键增加 **Show in FileSystem**，由 Godot 原生 FileSystem Dock 定位文件。
- 语义连线改为缓存的障碍规避折线路由；节点拖动或筛选使缓存失效，平移/缩放只变换图坐标路线。
- 实线、虚线、颜色与箭头语义在折线路由上保持不变，虚线相位跨折点连续。
- 新增长文件名 fixture、单障碍、连续双障碍、完整有机图全部边避障以及 editor 路由统计回归。

## 更新的需求 ID

- `[0.2.0-FE-A-033]` 至 `[0.2.0-FE-A-037]`

## 更新的 README 或 DESIGN

- README：拖拽手势、完整文件名、右键定位和节点避障说明。
- DESIGN：卡片显示边界、文件名换行、交互手势和 `14 px` 连线路由约束。
- ARCHITECTURE：可见性图、增量障碍发现、缓存失效与 FileSystem Dock 边界。
- CHANGELOG：本轮用户可见能力。

## 验证方式

- Godot 4.7 core：通过；7 个 fixture 节点、4 条扫描边。
- 核心回归继续保持：51 节点交叉 `101 → 32`，199 节点交叉 `4539 → 1018`，直接关联平均距离 `428.1 px`、非关联 `630.3 px`。
- 路由回归：单障碍、连续双障碍以及 51 节点有机样例的全部边都保持 `14 px` 卡片净空。
- Godot 4.7 main editor smoke：`9 graph nodes, 4/4 routed, 1 detours, 0 blocked`。
- Godot 4.7 隔离安装 smoke：`7 graph nodes, 4/4 routed, 1 detours, 0 blocked`，无 `SCRIPT ERROR`、`Parse Error` 或 `ERROR:`。
- `git diff --check`：通过。

## 备注

- 线与节点的相交现在是硬约束；任意非平面图的边与边零交叉仍然在数学上不可保证。
- `EditorInterface.select_file()` 是 Godot 4.4 官方提供的编辑器 API，满足插件最低兼容版本。
