# 正方形资产节点与选择拖拽

## 运行信息

- 用户原始 prompt：`名字不要用滚动，加高，允许显示至少 3 行字；资产类型的黑色部分缩小一点高度，做成整个是一个正方形的比例`
- 用户补充 prompt：`资产节点还是要能选中，我的意思是选中按着鼠标左键才能拖拽他们`
- 模型：GPT-5。当前运行环境只暴露到该模型标识，无法取得更细的公开版本号。
- 启动分支与版本：`main@db01a0f09a83`，与 `origin/main` 同步。
- 任务开始时间：2026-07-30 23:41:19 +0800。
- 任务结束时间：2026-07-30 23:44:43 +0800。
- 是否提交：是；按用户既有要求直接提交并推送 main。

## 已阅读上下文

- `AGENTS.md`、`README.md`、`REQUIREMENTS.md`、`DESIGN.md`。
- 上一轮画布拖拽、文件定位与节点避障日志。
- 当前 GraphNode 卡片、布局包围盒、editor smoke 与长文件名 fixture。

## 对话与行动记录

- 保持用户上一轮指定的 `320 px` 节点宽度，把高度加到相同尺寸形成正方形。
- 文件名不再放入 ScrollContainer；节点内容控件不截获左键，让 GraphNode 自身负责选择与拖拽。
- 显式设置 `selectable=true` 与 `draggable=true`；普通左键不由卡片自定义处理器消费，双击和右键仍保持原功能。

## 完成工作

- 卡片从 `320 × 190 px` 改为 `320 × 320 px`，布局默认尺寸、碰撞、边端点和节点避障同步使用正方形。
- 文件名改为直接挂在 GraphNode titlebar 的普通 Label，固定 `248 px` 高并按任意字符换行，不存在标题滚动容器。
- 底部资产类型区域改为 `42 px` 高的 CenterContainer 短条。
- 文件名、类型容器和类型文字均不拦截鼠标；GraphNode 明确保持可选中、可拖拽。
- editor smoke 增加实际正方形尺寸、标题三行最低高度、标题父容器、类型条实际高度、鼠标过滤以及 selectable/draggable 检查。

## 更新的需求 ID

- `[0.2.0-FE-A-038]` 至 `[0.2.0-FE-A-041]`

## 更新的 README 或 DESIGN

- README：正方形卡片、多行非滚动文件名、选择后左键拖拽。
- DESIGN：`320 × 320 px`、`248 px` 标题区、`42 px` 类型条和事件穿透规范。
- ARCHITECTURE：布局包围盒与 GraphNode 选择/拖拽边界。
- CHANGELOG：本轮卡片与交互调整。

## 验证方式

- Godot 4.7 core：通过；正方形布局样例交叉 `86 → 41`，199 节点大图 `3637 → 804`，关联平均距离 `512.1 px`、非关联 `793.9 px`。
- Godot 4.7 main editor smoke：`9 graph nodes, 4/4 routed, 0 detours, 0 blocked`。
- Godot 4.7 隔离安装 smoke：`7 graph nodes, 4/4 routed, 0 detours, 0 blocked`，无 `SCRIPT ERROR`、`Parse Error` 或 `ERROR:`。
- editor smoke 同时验证每张卡片为 `320 × 320 px`、标题至少三行高、无标题 ScrollContainer、类型条不高于 `43 px`、节点可选择和可拖拽。
- `git diff --check`：通过。

## 备注

- Godot 4.4 的 GraphElement 公共属性明确提供 `selectable`、`selected` 与 `draggable`；本实现显式设置前两项能力所需的 `selectable`、`draggable`，选中状态继续使用原生主题。
