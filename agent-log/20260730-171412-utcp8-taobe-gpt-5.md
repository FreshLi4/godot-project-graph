# 修复 main 并恢复完整新版 addon

## 运行信息

- 用户原始 prompt：`所有工作在 main 分支完成，之前 ai 合并似乎有些问题，先在 addon 里完全不是新版`
- 用户确认 prompt：`帮我修复，确保 main 是最新内容就行`
- 模型：GPT-5。当前运行环境只暴露到该模型标识，无法取得更细的公开版本号。
- 启动分支与版本：`main@34250e1`。
- 任务开始时间：2026-07-30，本轮收到用户请求时；运行环境未暴露消息的精确时分秒。
- 任务结束时间：2026-07-30 17:14:12 +0800。
- 是否提交：是；修复 main 分叉并将完整 visualization addon 合入 main。

## 已阅读上下文

- 当前 `AGENTS.md`、`README.md`、`REQUIREMENTS.md`、`DESIGN.md`、`docs/ARCHITECTURE.md`。
- `main`、`origin/main`、`origin/graph/visualization` 的提交图、文件清单与差异。
- visualization 分支的两条布局与 Ignore 执行日志。
- GitHub 发布工作流与本仓库 main 同步规则。

## 对话与行动记录

- 确认原仓库路径已经变成 worktree 容器，真正的 Git 仓库位于 `godot-project-graph_main/`。
- `git fetch --prune` 后发现本地 main ahead 5 / behind 1：
  - 本地 main 包含完整 Phase 1 与文档整理。
  - 远端 main 的单独提交只跟踪了 `.godot` 编辑器缓存，没有 Phase 1。
  - 完整 Organic Layout 与 Ignore 实现位于 `origin/graph/visualization@b64abe4`。
- 用户确认修复后，以非破坏性 merge 保留远端 main 历史，同时在合并树中删除可再生的 `.godot` 缓存。
- 合并 `origin/graph/visualization`，手工解决 `AGENTS.md`、`DESIGN.md` 与 `README.md` 冲突；保留 main 的中文精简结构并补齐新版功能。
- 未删除旁边的 Phase 1 普通目录副本，避免破坏仍可能被其他工具引用的本地文件。

## 完成工作

- main 现在完整包含 Phase 1 addon、Organic Layout、扫描 Ignore、自动居中与最新版测试。
- main 的 addon 与 `origin/graph/visualization` 对应 addon/test 文件比较为零差异。
- `.godot` 编辑器布局、导入缓存和 UID 缓存不再被 main 跟踪；`.gitignore` 继续排除 `.godot/`。
- `AGENTS.md` 明确 main 是唯一可分发基准，已交付功能不得只存在于 feature branch 或旁路副本。
- README 使用说明、DESIGN 有机布局规范、REQUIREMENTS 状态、ARCHITECTURE 与 CHANGELOG 已共同进入 main。

## 更新的需求 ID

- 保留并合入 `0.2.0-FE-A-006` 至 `0.2.0-FE-A-018` 的完整历史与完成状态。
- 本次是分支与发布基准修复，没有新增或重编号需求。

## 更新的 README 或 DESIGN 章节

- README：在精简中文结构中加入 Organic Layout、独立节点外置、Ignore 配置与默认扫描边界。
- DESIGN：保留有机力导向圆盘、软边界、Barnes–Hut、卡片碰撞和外围独立节点规范。
- AGENTS：新增 main 唯一分发基准约束。

## 验证方式

- GitHub CLI 认证：`github.com` 账号 `DDonlien` 可用。
- Godot 4.7 core：`PASS: Project Graph core (4 nodes, 3 edges)`。
- Godot 4.7 main editor smoke：`PASS: Project Graph editor panel smoke (6 graph nodes)`。
- Godot 4.7 隔离安装 smoke：只复制 `addons/` 与 fixture 到全新临时项目，`PASS: Project Graph editor panel smoke (4 graph nodes)`。
- 确认 main 中存在 `organic_graph_layout.gd`、`scan_ignore_settings.gd`，面板引用 `Organic Layout` 与 `Ignore…`。
- `git diff --cached --check`：通过。

## 备注

- 本次按用户明确要求直接修复并推送 main，不创建 feature branch 或 Draft PR。
- `.godot` 缓存的错误提交仍保留在 Git 父历史中以避免 force-push，但 main 最终文件树不包含这些缓存。
